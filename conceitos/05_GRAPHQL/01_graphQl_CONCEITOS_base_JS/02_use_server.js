/* eslint-disable no-unused-vars */

const server = {
  iniciar_server: ``,
  definir_rotas: `no arquivo schema.graphql`,
  tipos_de_rotas: `
  - do tipo query consulta:: type Query { props "_que vai ter" }
  - do tipo mutation mudanças:: type Mutation { recurso }`,
  tipos_de_retorno_das_rotas: `tambem declarados com o type Rota ex: type User { props que vai ter o retorno da rota User... }
  `,
};

const use_server_graphql = {
  gql: `gql é um modulo do apollo server e faz um parse de string da tipagem para o typeDefs`,
};
