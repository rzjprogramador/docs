super_admin_sudo: {
  conceito: `Se nao tiver permissao apra fazer algo use o sudo na frente do comando, vai conseguir fazer como super admin a maior hierarquia. ex: remover diretorio com : sudo rm -rf <Diretorio> `,
}

comandos_linux_debian: {
  deletar: {
    tudo_que_tiver_dentro: `rm -rf <Diretorio>`
  }
}

encontrar vscode instalado no linux :
/home/rzj/.vscode

encontrar extensoes vscode :: ~/.vscode/extensions

dar_permissao_escrita_arquivo_via_terminal: 'sudo chmod +x <ARQUIVO> ou Manualmente clicando com a esquerda no arquivo permissao/escriat e leitura',
